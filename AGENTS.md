# CQuant 项目约束

## 策略研究数据隔离

- 策略研究、假设形成、特征设计、参数选择和调参只允许使用
  `ts < 2025-06-01T00:00:00Z` 的历史数据。
- `ts >= 2025-06-01T00:00:00Z` 的数据统一作为验证集；在策略规则、参数和验证方案冻结前，
  不得读取或以任何形式用于研究决策。
- 时间边界必须固定为 `2025-06-01T00:00:00Z`（UTC），不得使用 `latest`、`now()` 或运行日
  动态替代。
- 代码中的切分边界以 `cq/research/split.py` 的 `FORWARD_FREEZE` 为单一事实源；新增研究不得
  另行定义不同的切分日期。

