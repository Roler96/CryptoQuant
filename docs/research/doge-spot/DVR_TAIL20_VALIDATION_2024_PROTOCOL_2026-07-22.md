# DOGE DVR-T20 2024 顺序验证预注册（2026-07-22）

> 本文件在首次运行 DVR-T20 的2024收益之前冻结。2024只用于验证这个唯一版本，
> 不再比较20%、25%、30% trailing，不增加过滤器，不调整仓位。

## 候选来源与诚实标签

预注册主版 DVR-T25 在2021–2023因“扣除最佳一笔后收益为负”而发现失败；同一份预注册
风险邻域中，T20为 `+85.82%`、Sharpe `0.72`、MaxDD `19.71%`、21笔、扣除最佳一笔
`+8.04%`。因此 T20 是根据 discovery 结果事后晋级的新版本，不追溯声称为预注册主版。
它新增第40次家族尝试。2024此前没有被任何新候选脚本读取，承担第一次顺序验证。

## 完全冻结的策略

- 实现：`cq/strategy/doge_dvr.py::DogeDvrTail20`；
- horizon 28日；DVR进入阈值0.60、退出阈值0.45；
- 目标现货仓位25%；峰值闭合close回撤20%时下一日open退出；
- trailing退出后必须等原DVR状态重置才可再次armed；
- 只用OKX `DOGE-USDT` spot OHLCV，完整UTC日线；
- 闭合日线决策，下一日open成交，`Sizing.ON_ENTRY`；
- 主成本每边fee10bps + slippage5bps；压力成本每边25bps；
- 初资10,000 USDT；随机种子 `20260722`。

冻结指纹：

- Git base: `b3e619cbd885dbc55311c94be390d4be3008e4af`（工作区含本研究未提交文件）；
- strategy source SHA256:
  `3d0843ebe4df1d217fb24f90b2b03e3faa636474decb586a55ba16127db8adba`；
- validation runner SHA256:
  `bc28b2d34d3f7d2ed614d17425d5d03da79e79048eb070a5c6fdc47527225b01`；
- runner：`research/validate_doge_dvr_tail20_2024.py`。

若上述两个源码哈希在首次运行前改变，本预注册作废并必须生成新版本。

## 数据隔离

- 进程只查询排他 `end_ms=2025-01-01 00:00 UTC`；
- 2021–2023仅作指标warmup，`WindowedDvr`在2024以前强制现金并重置内部状态；
- 评价区间固定 `2024-01-01 .. 2025-01-01`，年末未平仓按最后close MTM并明确报告；
- 本阶段不得加载2025或2026；若验证失败，版本归档，不打开后续年度。

## 2024通过门

必须全部满足：

- 15 bps/边后收益为正；
- Sharpe `>= 0.40`；
- MaxDD `<= 25%`；
- 25 bps/边压力后收益为正；
- 静态扣除最佳闭合交易 PnL 后收益为正；
- 至少4笔闭合交易。

bootstrap只报告，不因单年样本少而设为硬门。若通过，必须在打开2025前再写唯一最终版的
pre-freeze audit协议；2025只读一次，失败则不得打开2026。若通过，也不允许根据2024
修改任何参数。
